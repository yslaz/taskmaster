# ==============================================
# TASKMASTER - DOCKER COMPOSE CONFIGURATION
# ==============================================
# Copia este archivo como .env y configura las variables

# ==============================================
# REQUERIDO - CONFIGURACIÓN BÁSICA
# ==============================================

# Contraseña para PostgreSQL (OBLIGATORIO)
DB_PASSWORD=password

# Secreto JWT para firmar tokens (OBLIGATORIO)
JWT_SECRET=jwt_secret

# ==============================================
# OPCIONAL - CONFIGURACIÓN DE BASE DE DATOS
# ==============================================

# Configuración de PostgreSQL
POSTGRES_DB=taskmaster
POSTGRES_USER=admin
POSTGRES_PORT=5432

# Pool de conexiones de base de datos
DB_MAX_CONNECTIONS=10
DB_MIN_CONNECTIONS=2
DB_ACQUIRE_TIMEOUT=60
DB_IDLE_TIMEOUT=600
DB_MAX_LIFETIME=1800
DB_CONNECT_TIMEOUT=30

# ==============================================
# OPCIONAL - CONFIGURACIÓN DEL SERVIDOR
# ==============================================

# Puerto del backend API
BACKEND_PORT=8000

# Configuración JWT
JWT_EXPIRATION_HOURS=24

# CORS - URLs permitidas (* para desarrollo)
CORS_ORIGIN=*

# Nivel de logging
RUST_LOG=taskmaster_backend=info,actix_web=info,sqlx=warn

# ==============================================
# INSTRUCCIONES DE USO
# ==============================================
# 1. Copia este archivo: cp .env.example .env
# 2. Edita .env con tus valores reales
# 3. Configura también backend/.env (ver backend/.env.example)
# 4. Ejecuta: docker-compose up -d
# 5. API disponible en: http://localhost:8000